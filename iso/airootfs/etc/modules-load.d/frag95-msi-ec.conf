# Load the MSI embedded-controller driver at boot so Frag95 performance profiles
# (fan mode, shift mode, Cooler Boost) work on MSI laptops. The module refuses
# to load on non-MSI / unsupported-firmware machines, so this is a safe no-op
# everywhere else.
msi-ec
