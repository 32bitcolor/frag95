# Apply the GPU profile to the target. "auto" detects via the Phase 2
# /usr/share/frag95/gpu/auto/detect.sh. A future interactive GPU-choice page
# will pass the user's pick here instead of "auto".
---
dontChroot: false
timeout: 600
script:
    - "/usr/local/bin/frag95-apply-gpu.sh auto"
