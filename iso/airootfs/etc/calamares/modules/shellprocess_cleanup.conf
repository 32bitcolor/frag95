# Turn the unpacked live rootfs into a proper installed system (drop the live
# user / autologin / passwordless sudo / installer launcher).
---
dontChroot: false
timeout: 60
script:
    - "/usr/local/bin/frag95-cleanup-install.sh"
