# Frag95 Hybrid/Optimus profile: Intel/AMD iGPU drives the displays, NVIDIA dGPU
# is used on demand via PRIME render offload. The iGPU driver (i915/amdgpu) is
# in-kernel; here we enable NVIDIA DRM modeset so the dGPU is offload-ready.
options nvidia_drm modeset=1 fbdev=1
