# Frag95 NVIDIA legacy profile (nvidia-470xx, pre-Turing GPUs). The 470xx branch
# supports DRM KMS; modeset=1 is needed for a clean console/X handoff. fbdev is
# left off — the legacy branch's fbdev support is less reliable than current.
options nvidia_drm modeset=1
