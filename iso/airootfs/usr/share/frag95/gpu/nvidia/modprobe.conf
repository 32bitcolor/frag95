# Frag95 NVIDIA profile (applied to the installed system by the Phase 6 installer).
# DRM kernel mode setting is required for a working Wayland session and for a
# clean X11 handoff; fbdev gives a flicker-free console on the dGPU.
options nvidia_drm modeset=1 fbdev=1
